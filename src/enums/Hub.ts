export enum UpcStatus {
    OK,
    MISMATCHED,
    INVALID
}

export enum ReportStatus {
    NONE,
    OPEN,
    CLOSED,
    APPROVED,
    REJECTED,
    MOVETOHOLDZONE,
    FORCERECEIVE,
    RECEIVE,
    ACKNOWLEDGE,
    OVERRIDEFOOTPRINT
}